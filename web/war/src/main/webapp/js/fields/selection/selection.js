
define(['util/ontology/propertySelect'], function(PropertySelect) {
    'use strict';

    console.warn('fields/selection/selection has been moved to util/ontology/propertySelect');

    return PropertySelect;
});
