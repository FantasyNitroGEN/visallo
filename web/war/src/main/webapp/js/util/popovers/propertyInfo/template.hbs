<div class="propertyInfoPopover popover top">
  <div class="arrow"></div>
  <div class="popover-content">
    <table class="table"></table>
    <ul class="buttons">

    {{#unless isComment}}
      {{#if canDelete}}
      <li><button class="btn btn-danger btn-small">{{ i18n 'popovers.property_info.button.delete' }}</button></li>
      {{/if}}

      {{#if canSearch}}
      <li><button class="btn btn-search btn-small">{{ i18n 'popovers.property_info.button.search' }}</button></li>
      {{/if}}

      {{#if canEdit}}
      <li><button class="btn-edit btn btn-small">{{ i18n 'popovers.property_info.button.edit' }}</button></li>
      {{else}}
          {{#if canAdd}}
            <li><button class="btn-add btn btn-small">{{ i18n 'popovers.property_info.button.add' }}</button></li>
          {{/if}}
      {{/if}}
    {{/unless}}

    {{#if isComment}}
      {{#if canDelete}}
      <li><button class="btn btn-danger btn-small">{{ i18n 'popovers.property_info.button.delete' }}</button></li>
      {{/if}}
      {{#if canEdit}}
      <li><button class="editadd btn btn-small btn-edit">{{ i18n 'popovers.property_info.button.edit' }}</button></li>
      {{/if}}
      {{#if canAdd}}
      <li><button class="reply btn btn-small">{{ i18n 'popovers.property_info.button.reply' }}</button></li>
      {{/if}}
    {{/if}}
    </ul>
  </div>
</div>
