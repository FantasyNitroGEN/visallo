
<div class="entity-background">
  <div class="entity-glyphIcon" style="background-image: url(<%= concept.glyphIconHref %>)"></div>
  <h1 class="generic-draggable artifact-title" 
    data-vertex-id='<%= vertex.id %>'><%= F.vertex.title(vertex) %></h1>
  <div class="subtitle"><%= concept.displayName %></div>

  <div class="comp-toolbar"></div>
</div>

<div class="nav-with-background">
  <div class="delete-form"></div>

  <% if(F.vertex.displayType(vertex) == 'video') { %>
    <div class="preview"/>
    <div class="currentTranscript"></div>
  <% } %>
  <% if(F.vertex.displayType(vertex) == 'image') { %>
    <div class="image-preview opens-dropdown"></div>
  <% } %>
  <% if(F.vertex.displayType(vertex) == 'audio') { %>
    <div class="audio-preview"></div>
    <div class="currentTranscript"></div>
  <% } %>

  <div class='detected-object-labels'></div>

  <div class="artifact-header">
    <div class="properties"></div>
    <div class="relationships"></div>
    <div class="comments"></div>
  </div>

  <div class="texts"></div>
</div>
